#version 330
layout (location = 0) in vec2 position;
layout (location = 1) in vec3 color;

out vec2 outPosition;
out vec3 outColor;

out mat4 textMatrix;

void main()
{
	gl_Position = textMatrix * vec4(position, 0.0, 1.0);
	outColor = color;
}